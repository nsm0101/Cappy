# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example

# Test artifacts
coverage/
*.lcov
.nyc_output/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor
.vscode/
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# iOS
ios/build/
ios/DerivedData/
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
ios/*.xcodeproj/project.xcworkspace/xcuserdata/
ios/Pods/

# Android (when built)
android/build/
android/.gradle/
android/local.properties
android/app/build/
android/captures/
android/app/release/
android/app/debug/

# Compliance — never commit signed PDFs of BAAs to git
compliance/baas/*.pdf

# Secrets — defensive
*.pem
*.key
*.p12
*.mobileprovision
*.cer

# Output of database dumps
*.sql.gz
*.dump

# Temp
.tmp/
tmp/
.DS_Store

# Signing material — NEVER commit.
# APNs auth keys download exactly once and cannot be re-downloaded; a leaked
# one lets anyone push notifications to every app in the team until it is
# revoked. Checked in 2026-09-03 after two .p8 files were found untracked but
# un-ignored under "apple dev/", one `git add -A` away from being published.
*.p8
*.p12
*.pem
*.mobileprovision
*.cer
*.certSigningRequest
AuthKey_*
apple dev/
